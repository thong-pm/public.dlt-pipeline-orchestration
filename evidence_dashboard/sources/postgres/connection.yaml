name: postgres
type: ${EVIDENCE_DB_TYPE}
options:
  host: ${EVIDENCE_DB_HOST}
  port: ${EVIDENCE_DB_PORT}
  database: ${EVIDENCE_DB_NAME}
  user: ${EVIDENCE_DB_USER}
  password: ${EVIDENCE_DB_PASSWORD}
  ssl: ${EVIDENCE_DB_SSL}
  projectId: ${GCP_PROJECT_ID}
  dataset: ${EVIDENCE_BQ_DATASET}
